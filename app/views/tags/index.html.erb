<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, article_path(tag) %>
    </li>
  <% end %>
</ul>
<%= link_to "<< Back to Articles List", articles_path %>